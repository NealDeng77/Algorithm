//Permutation. Given two integer arrays of size nn, design a subquadratic algorithm to 
//determine whether one is a permutation of the other. That is, do they contain exactly 
//the same entries but, possibly, in a different order.

/*
 * The same idea as intersection and union or two arrays.
 * First sort two arrays, then check the length, then check each elements
 */
public class Permutation {

}
